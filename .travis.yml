language: node_js
node_js:
  - 8

branches:
  only:
    - master
    - dev

sudo: false

install:
  - npm install riot-compiler
  - npm i

cache:
  directories:
    - node_modules

notifications:
    email: false